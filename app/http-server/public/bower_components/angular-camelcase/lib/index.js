// Generated by CoffeeScript 1.9.1
var capitalizeFirstLetter;

capitalizeFirstLetter = function(str) {
  if (str.length === 0) {
    return '';
  }
  return "" + (str.substr(0, 1).toUpperCase()) + (str.substr(1));
};

module.exports = angular.module('filters.camelcase', []).filter('toCamelCase', function() {
  return function(string) {
    var firstToken, otherTokens, tokens;
    tokens = string.split(" ");
    if (tokens.length === 0) {
      return '';
    }
    firstToken = tokens[0];
    otherTokens = tokens.slice(1);
    return "" + (firstToken.toLowerCase()) + (otherTokens.map(capitalizeFirstLetter).join(''));
  };
}).filter('titleFromCamelCase', function() {
  return function(camelCase) {
    return camelCase.replace(/([A-Z]+)/g, ' $1').replace(/^./, function(str) {
      return str.toUpperCase();
    }).trim();
  };
});
